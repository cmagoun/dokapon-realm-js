import React from 'react';
import ItemsDialog from "../ui/ItemsDialog";
import PowersDialog from '../ui/PowersDialog';
import SpellsDialog from '../ui/SpellsDialog';

export const tileSize = 48;

export const Dialogs = {
    itemsdialog: <ItemsDialog/>,
    powersdialog: <PowersDialog/>,
    spellsdialog: <SpellsDialog/>
}